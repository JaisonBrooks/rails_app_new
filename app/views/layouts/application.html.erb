<!DOCTYPE html>
<html>
  <head>
    <title><%="#{content_for?(:title) ? "#{yield(:title)} | " : ''}Vibin Veggies"%></title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_pack_tag 'application' %>
  </head>

  <body>
    <%= yield %>
    <%= javascript_pack_tag 'application' %>
  </body>
</html>
